#!/usr/bin/env python3
"""Check client derivations against gwnative's fail-closed build registry."""

from pathlib import Path
import sys

sys.path.insert(0, str(Path(__file__).resolve().parent.parent))

from tools.client_analyzer import certify_main

raise SystemExit(certify_main())
